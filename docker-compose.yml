version: '3'

services:
  mysql:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3306:3306'
    volumes:
      - ./data:/var/lib/mysql
      - ./mydatabase.sql:/docker-entrypoint-initdb.d/mydatabase.sql